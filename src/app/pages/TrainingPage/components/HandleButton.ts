import styled from 'styled-components';

const HandleButton = styled.button`
	padding: 9px 25px;

	align-self: center;

	background: #0082ca;
	box-shadow: 0px 0px 15px rgba(0, 130, 202, 0.1);
	border: none;
	border-radius: 25px;
	outline: none;
	cursor: pointer;

	font-family: 'Open Sans', sans-serif;
	font-size: 16px;
	color: #ffffff;
`;

export default HandleButton;
