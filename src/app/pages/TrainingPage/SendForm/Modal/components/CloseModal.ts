import styled from 'styled-components';
import { CloseSelectedFile } from '../../FileLoader/components';

const CloseModal = styled(CloseSelectedFile)`
	position: absolute;
	top: 10px;
	right: 10px;
`;

export default CloseModal;
