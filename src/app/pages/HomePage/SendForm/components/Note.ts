import styled from 'styled-components';

const Note = styled.p`
	font-family: Open Sans;
	font-size: 12px;
	font-style: normal;
	font-weight: 300;
	line-height: 12px;

	color: #4e4e4e;
`;

export default Note;
