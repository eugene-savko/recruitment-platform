import styled from 'styled-components';

const Label = styled.label`
	font-family: Roboto;
	font-style: normal;
	font-weight: normal;
	font-size: 12px;
	line-height: 14px;

	color: #4e4e4e;

	cursor: pointer;
`;

export default Label;
