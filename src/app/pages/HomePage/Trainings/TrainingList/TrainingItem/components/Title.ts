import styled from 'styled-components';

const Title = styled.h2`
	margin: 0 0 15px 0;
	font-style: normal;
	font-weight: normal;
	font-size: 16px;
	line-height: 16px;
	color: #222222;
`;

export default Title;
