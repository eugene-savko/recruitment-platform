import styled from 'styled-components';

import ArrowForwardIcon from '@material-ui/icons/ArrowForward';

export const ScheduleButtonLeave = styled(ArrowForwardIcon)`
	cursor: pointer;
	fill: #6b778c;
`;
