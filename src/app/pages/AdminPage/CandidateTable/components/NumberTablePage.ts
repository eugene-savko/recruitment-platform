import styled from 'styled-components';
import { Typography } from '@material-ui/core';

const NumberTablePage = styled(Typography)`
	font-family: Inter, sans-serif;
	font-style: normal;
	font-weight: 400;
	font-size: 14px;
	line-height: 15px;
	letter-spacing: 0.05em;
	color: #3f51b5;
`;
export default NumberTablePage;
