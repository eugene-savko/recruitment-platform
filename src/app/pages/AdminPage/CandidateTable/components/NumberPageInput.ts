import styled from 'styled-components';
import { Input } from '@material-ui/core';

const NumberPageInput = styled(Input)`
	border: 1px solid lightgray;
	padding-left: 5px;
	width: 40px;
	border-radius: 4px;
`;
export default NumberPageInput;
