import styled from 'styled-components';
import { Button as MaterialButton } from '@material-ui/core';

const FilterButton = styled(MaterialButton)`
	margin: 20px 20px 0 0;
`;
export default FilterButton;
