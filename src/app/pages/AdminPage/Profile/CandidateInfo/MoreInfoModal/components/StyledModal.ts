import styled from 'styled-components';

const StyledModal = styled.div`
	position: relative;
	z-index: 100;
	margin: auto;
	padding: 10px;
	background: white;
	border-radius: 8px;
`;

export default StyledModal;
