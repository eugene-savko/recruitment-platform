import { TableCell as MaterialCell } from '@material-ui/core';
import styled from 'styled-components';

const TableCell = styled(MaterialCell)`
	padding-top: 12px;
	padding-bottom: 12px;
	text-align: center;
	color: #000;
`;
export default TableCell;
