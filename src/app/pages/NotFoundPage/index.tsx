import React from 'react';
import styled from 'styled-components';

const NotFoundTitle = styled.h1`
	text-align: center;
	margin: 80px auto;
`;

export const NotFoundPage: React.FunctionComponent = () => (
	<NotFoundTitle>NotFoundPage</NotFoundTitle>
);
