import styled from 'styled-components';

const LinkOfFooter = styled.a`
	font-family: 'Roboto', 'Helvetica', 'Arial', sans-serif;
	font-size: 10px;
	font-weight: 400;
	color: #222;

	text-decoration: none;

	&:hover {
		color: #0082ca;
		text-decoration: underline;
	}
`;

export default LinkOfFooter;
