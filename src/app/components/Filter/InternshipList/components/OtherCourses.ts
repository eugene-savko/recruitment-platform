import styled from 'styled-components';

export const OtherCourses = styled.button`
	margin: 22px 0;
	border: none;
	&:focus {
		outline: none;
	}
	font-size: 16px;
	font-weight: 600;
	background-color: #fafafa;
	color: rgba(0, 130, 202, 1);
`;
