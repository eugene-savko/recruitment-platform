import React from 'react';
import { SButton } from 'app/components/Auth/styled/style';

export const ButtonSubmit: React.FC = () => (
	<SButton type="submit" color="primary" variant="contained" fullWidth>
		Войти
	</SButton>
);
